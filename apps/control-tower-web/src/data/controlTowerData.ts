import { dashboardMetrics, heatmap, portalActions, rolePermissions, shipments, slaWidgets } from "@cxtms/shared";

export const controlTowerSeed = {
  shipments,
  dashboardMetrics,
  heatmap,
  slaWidgets,
  portalActions,
  rolePermissions
};
